# Infu PL Standard Version 1.0

> Type:

***Interpret***, ***Generate (cpp)***, ***REPL***

> Keyword:

**use**, **imp**, **ret**, **var***(no type,array,const)*, **fun***(no type,array)*, **rep**, **when**, **if**, **com**.

> Library:

**std.io***(call sys.io)*, **std.tool**, **std.call***(only shell,call sys.os)*, **std.math**, **std.rand***(call sys.rand)*

> System:

**sys.io**, **sys.os**, **sys.rand**

> Addition:
